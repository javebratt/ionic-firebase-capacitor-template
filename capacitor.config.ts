import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-firebase-capacitor-starter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
